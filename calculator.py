def add(a: float, b: float) -> float:
    """
    Сложение двух чисел.

    Args:
        a: Первое число
        b: Второе число

    Returns:
        Сумма a и b
    """
    return a + b


def subtract(a: float, b: float) -> float:
    """
    Вычитание двух чисел.

    Args:
        a: Уменьшаемое
        b: Вычитаемое

    Returns:
        Разность a и b
    """
    return a - b


def multiply(a: float, b: float) -> float:
    """
    Умножение двух чисел.

    Args:
        a: Первый множитель
        b: Второй множитель

    Returns:
        Произведение a и b
    """
    return a * b


def divide(a: float, b: float) -> float:
    """
    Деление двух чисел.

    Args:
        a: Делимое
        b: Делитель

    Returns:
        Частное a и b

    Raises:
        ValueError: Если делитель равен 0
    """
    if b == 0:
        raise ValueError("Деление на ноль невозможно")
    return a / b


# Дополнительно: функция для демонстрации работы
def main():
    """
    Демонстрация работы калькулятора.
    """
    print("🧮 Демонстрация работы калькулятора:")
    print("=" * 40)

    # Тестовые примеры
    examples = [
        ("5 + 3", add(5, 3), 8),
        ("10 - 4", subtract(10, 4), 6),
        ("7 * 6", multiply(7, 6), 42),
        ("15 / 3", divide(15, 3), 5),
    ]

    for operation, result, expected in examples:
        status = "✓" if result == expected else "✗"
        print(f"{status} {operation} = {result}")

    print("\n🔍 Проверка обработки ошибок:")
    print("=" * 40)

    try:
        divide(10, 0)
        print("✗ Деление на ноль: Ошибка не сработала!")
    except ValueError as e:
        print(f"✓ Деление на ноль: {e}")


if __name__ == "__main__":
    main()
